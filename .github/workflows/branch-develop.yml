name: "Branch: develop"

on:
  push:
    branches:
      - develop

permissions:
  contents: read

concurrency:
  group: branch-develop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validation:
    name: Run full CI
    uses: ./.github/workflows/components.yml
    with:
      os_line: os2
      include_docs: true
      artifact_retention_days: 7

  full_ci:
    name: Full CI
    needs: validation
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check full CI result
        run: |
          if [[ "${{ needs.validation.result }}" != "success" ]]; then
            echo "Full CI failed: ${{ needs.validation.result }}" >&2
            exit 1
          fi

  deploy_docs:
    name: Deploy develop docs
    needs: full_ci
    if: ${{ needs.full_ci.result == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: docs-deploy
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install docs dependencies
        run: python -m pip install 'mkdocs<2' mkdocs-material pymdown-extensions mkdocs-drawio mike
      - name: Deploy develop docs with mike
        run: scripts/deploy-docs-version.sh --version develop --set-default
