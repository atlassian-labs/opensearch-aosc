name: Components

on:
  workflow_call:
    inputs:
      os_line:
        description: OpenSearch compatibility line (os2 | os3 | all)
        required: false
        default: os3
        type: string
      tier:
        description: Test tier — fast (fastCheck + yamlRestTest) or full (all suites)
        required: false
        default: full
        type: string
      include_docs:
        description: Build documentation as part of validation
        required: false
        default: true
        type: boolean
      artifact_retention_days:
        description: Retention for failure artifacts
        required: false
        default: 7
        type: number
      ref:
        description: "Git ref/SHA to check out; empty uses the triggering ref"
        required: false
        default: ''
        type: string

permissions:
  contents: read

jobs:
  metadata:
    name: Validation metadata
    runs-on: ubuntu-latest
    outputs:
      validation_matrix_json: ${{ steps.matrix.outputs.validation_matrix_json }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      - id: matrix
        env:
          VALIDATION_TIER: ${{ inputs.tier }}
        run: ./scripts/ci-metadata.py "${{ inputs.os_line }}" --github-output

  docs:
    name: Docs build
    needs: metadata
    if: inputs.include_docs
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
      - name: Install docs dependencies
        run: npm ci
      - name: Build docs
        run: npm run docs:build

  gradle:
    name: ${{ matrix.label }} (OpenSearch ${{ matrix.opensearch_version }})
    needs: metadata
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.metadata.outputs.validation_matrix_json) }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ matrix.build_java_version }}
          cache: gradle
      - name: Select Gradle for the OpenSearch version
        run: ./scripts/set-gradle.sh "${{ matrix.opensearch_version }}"
      - name: Pre-fetch Gradle
        run: ./scripts/ensure-gradle.sh
      - name: Run ${{ matrix.label }}
        env:
          OPENSEARCH_VERSION: ${{ matrix.opensearch_version }}
          GRADLE_ARGS: ${{ matrix.gradle_args }}
          GRADLE_TASK: ${{ matrix.gradle_task }}
        run: |
          args=("-PopensearchVersion=${OPENSEARCH_VERSION}")
          if [[ -n "${GRADLE_ARGS}" ]]; then
            read -r -a extra_args <<< "${GRADLE_ARGS}"
            args+=("${extra_args[@]}")
          fi
          ./scripts/gradle-with-retry.sh ./gradlew --no-daemon --continue "${args[@]}" "${GRADLE_TASK}"
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: validation-${{ matrix.os_line }}-${{ matrix.id }}-${{ matrix.opensearch_version }}-${{ github.run_attempt }}
          path: |
            aosc-plugin/build/reports/**
            aosc-plugin/build/test-results/**
          if-no-files-found: ignore
          retention-days: ${{ inputs.artifact_retention_days }}

  result:
    name: Validation result
    needs:
      - metadata
      - docs
      - gradle
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check reusable validation result
        run: |
          if [[ "${{ needs.metadata.result }}" != "success" ]]; then
            echo "Validation metadata failed: ${{ needs.metadata.result }}" >&2
            exit 1
          fi
          if [[ "${{ inputs.include_docs }}" == "true" && "${{ needs.docs.result }}" != "success" ]]; then
            echo "Docs validation failed: ${{ needs.docs.result }}" >&2
            exit 1
          fi
          if [[ "${{ needs.gradle.result }}" != "success" ]]; then
            echo "Gradle validation failed: ${{ needs.gradle.result }}" >&2
            exit 1
          fi
