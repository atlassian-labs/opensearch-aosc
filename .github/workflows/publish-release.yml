name: Publish Release

on:
  workflow_dispatch:
    inputs:
      os_line:
        description: OpenSearch compatibility line
        required: true
        default: os3
        type: choice
        options:
          - os2
          - os3
      prerelease:
        description: Mark draft release as prerelease
        required: true
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: publish-release-${{ github.ref_name }}-${{ inputs.os_line }}
  cancel-in-progress: false

jobs:
  metadata:
    name: Metadata
    runs-on: ubuntu-latest
    outputs:
      aosc_version: ${{ steps.matrix.outputs.aosc_version }}
      os_line: ${{ steps.matrix.outputs.os_line }}
      java_version: ${{ steps.matrix.outputs.java_version }}
      release_branch: ${{ steps.matrix.outputs.release_branch }}
      primary_version: ${{ steps.matrix.outputs.primary_version }}
      build_versions: ${{ steps.matrix.outputs.build_versions }}
      test_versions: ${{ steps.matrix.outputs.test_versions }}
      release_tag: ${{ steps.matrix.outputs.release_tag }}
      docs_version: ${{ steps.matrix.outputs.docs_version }}
      build_versions_json: ${{ steps.matrix.outputs.build_versions_json }}
      test_versions_json: ${{ steps.matrix.outputs.test_versions_json }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}
      - id: matrix
        run: ./scripts/github-ci-matrix.sh "${{ inputs.os_line }}" --github-output
      - name: Check release state
        env:
          AOSC_VERSION: ${{ steps.matrix.outputs.aosc_version }}
          RELEASE_BRANCH: ${{ steps.matrix.outputs.release_branch }}
          RELEASE_TAG: ${{ steps.matrix.outputs.release_tag }}
          GH_TOKEN: ${{ github.token }}
        run: |
          if [[ "${GITHUB_REF_NAME}" != "${RELEASE_BRANCH}" ]]; then
            echo "Release line ${{ steps.matrix.outputs.os_line }} must be published from ${RELEASE_BRANCH}; current ref is ${GITHUB_REF_NAME}" >&2
            exit 1
          fi
          if [[ ! "${AOSC_VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Release publishing requires aosc.version to be exact X.Y.Z; found ${AOSC_VERSION}" >&2
            exit 1
          fi
          if gh release view "${RELEASE_TAG}" >/dev/null 2>&1; then
            echo "GitHub release ${RELEASE_TAG} already exists." >&2
            exit 1
          fi
          if git ls-remote --exit-code --tags origin "refs/tags/${RELEASE_TAG}" >/dev/null 2>&1; then
            echo "Git tag ${RELEASE_TAG} already exists." >&2
            exit 1
          fi

  validation:
    name: Release validation
    needs: metadata
    uses: ./.github/workflows/components.yml
    with:
      os_line: ${{ needs.metadata.outputs.os_line }}
      include_docs: true
      artifact_retention_days: 14

  build:
    name: Build release ZIPs
    needs:
      - metadata
      - validation
    if: ${{ always() && needs.metadata.result == 'success' && needs.validation.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ needs.metadata.outputs.java_version }}
          cache: gradle
      - name: Pre-fetch Gradle
        run: ./scripts/ensure-gradle.sh
      - name: Build release ZIPs
        run: ./scripts/build-release-zips.sh "${{ needs.metadata.outputs.os_line }}"
      - name: Generate release notes stub
        run: |
          {
            echo "# ${{ needs.metadata.outputs.release_tag }}"
            echo
            echo "## Compatibility"
            echo
            echo "This release contains AOSC ${{ needs.metadata.outputs.aosc_version }} builds for ${{ needs.metadata.outputs.os_line }}."
            echo
            echo "Build versions: ${{ needs.metadata.outputs.build_versions }}"
            echo
            echo "Test versions: ${{ needs.metadata.outputs.test_versions }}"
            echo
            echo "Documentation: https://atlassian-labs.github.io/opensearch-aosc/${{ needs.metadata.outputs.docs_version }}/"
            echo
            echo "Release branch: ${GITHUB_REF_NAME}"
            echo
            echo "Source commit: ${GITHUB_SHA}"
            echo
            echo "See \`SHA256SUMS\` for asset checksums."
          } > build/release/RELEASE_NOTES.md
      - uses: actions/upload-artifact@v7
        with:
          name: release-assets-${{ needs.metadata.outputs.release_tag }}-${{ github.run_attempt }}
          path: |
            build/release/*.zip
            build/release/SHA256SUMS
            build/release/RELEASE_NOTES.md
            build/release/release.env
          if-no-files-found: error
          retention-days: 14

  draft-release:
    name: Create draft GitHub release
    needs:
      - metadata
      - build
    if: ${{ always() && needs.metadata.result == 'success' && needs.build.result == 'success' }}
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      attestations: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v8
        with:
          name: release-assets-${{ needs.metadata.outputs.release_tag }}-${{ github.run_attempt }}
          path: build/release
      - name: Attest release ZIPs
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: build/release/*.zip
      - name: Create draft GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.metadata.outputs.release_tag }}
        run: |
          git fetch --tags origin
          git tag "${RELEASE_TAG}" "${GITHUB_SHA}"
          git push origin "refs/tags/${RELEASE_TAG}"
          FLAGS=(--draft --target "${GITHUB_SHA}" --title "${RELEASE_TAG}" --notes-file build/release/RELEASE_NOTES.md)
          if [[ "${{ inputs.prerelease }}" == "true" ]]; then
            FLAGS+=(--prerelease)
          fi
          gh release create "${RELEASE_TAG}" build/release/*.zip build/release/SHA256SUMS "${FLAGS[@]}"

  deploy_docs:
    name: Deploy release docs
    needs:
      - metadata
      - build
      - draft-release
    if: ${{ always() && needs.metadata.result == 'success' && needs.build.result == 'success' && needs.draft-release.result == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: docs-deploy
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
      - name: Install docs dependencies
        run: npm ci
      - name: Deploy release docs
        run: scripts/deploy-docs-version.sh --version "${{ needs.metadata.outputs.docs_version }}"
