
# intellij files
.idea/
*.iml
*.ipr
*.iws
build-idea/
out/

# include shared intellij config
!.idea/inspectionProfiles/Project_Default.xml
!.idea/runConfigurations/Debug_OpenSearch.xml

# These files are generated in the main tree by IntelliJ
benchmarks/src/main/generated/*

# eclipse files
.project
.classpath
.settings
build-eclipse/

# netbeans files
nb-configuration.xml
nbactions.xml

# gradle stuff
.gradle/
build/
bin/

# vscode stuff
.vscode/

# testing stuff
**/.local*
.vagrant/
/logs/

# osx stuff
.DS_Store

# default folders in which the create_bwc_index.py expects to find old es versions in
/backwards
/dev-tools/backwards

# needed in case docs build is run...maybe we can configure doc build to generate files under build?
html_docs
node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/
npm-debug.log*

# random old stuff that we should look at the necessity of...
/tmp/
eclipse-build

# projects using testfixtures
testfixtures_shared/

# These are generated from .ci/jobs.t
.ci/jobs/

# ASDF for Java version management
.tool-versions

# VS Code workspace
*.code-workspace

# Docker build artifact (copied from build/distributions/ during deploy)
opensearch-docker/opensearch-aosc.zip

# Benchmark results (machine-specific, not reproducible)
benchmarks/results/*
!benchmarks/results/.gitkeep


# Python bytecode cache
__pycache__/
*.pyc
site/
opensearch-docker/opensearch-aosc.zip
# direnv local env (per-developer, not committed)
.envrc
