import{_ as t,o as n,c as o,a2 as i}from"./chunks/framework.C_Knpfj2.js";const m=JSON.parse('{"title":"Extension Points","description":"","frontmatter":{},"headers":[],"relativePath":"contributing/extensions.md","filePath":"contributing/extensions.md","lastUpdated":1781721505000}'),a={name:"contributing/extensions.md"};function r(s,e,c,l,p,d){return n(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="extension-points" tabindex="-1">Extension Points <a class="header-anchor" href="#extension-points" aria-label="Permalink to &quot;Extension Points&quot;">​</a></h1><p>AOSC exposes a small extension surface for plugins that need to customize migration behavior while reusing the base coordinator, worker, and REST machinery.</p><p>These APIs should be treated as public compatibility surfaces within a supported OpenSearch major line.</p><h2 id="aoscplugin" tabindex="-1"><code>AoscPlugin</code> <a class="header-anchor" href="#aoscplugin" aria-label="Permalink to &quot;`AoscPlugin`&quot;">​</a></h2><p><code>AoscPlugin</code> is the base OpenSearch plugin entry point. Extension plugins may subclass it to customize component wiring while preserving the AOSC REST and transport action surface.</p><p>Compatibility expectations:</p><ul><li>constructors and protected factory methods used by extension plugins should remain source-compatible within a supported OpenSearch major line</li><li>incompatible changes require release notes and a major compatibility-line decision</li><li>extension plugins should keep the installed plugin identity compatible with the deployment model they target</li></ul><h2 id="transformfactory" tabindex="-1"><code>TransformFactory</code> <a class="header-anchor" href="#transformfactory" aria-label="Permalink to &quot;`TransformFactory`&quot;">​</a></h2><p><code>TransformFactory</code> creates the <code>TransformFunction</code> used during backfill and replay.</p><p>Extension plugins may override transform creation to support custom script contexts or transform behavior. Implementations should preserve the base behavior for existing AOSC transform modes unless intentionally replacing them.</p><h2 id="transformfunction" tabindex="-1"><code>TransformFunction</code> <a class="header-anchor" href="#transformfunction" aria-label="Permalink to &quot;`TransformFunction`&quot;">​</a></h2><p><code>TransformFunction</code> receives an <code>IndexDoc</code> and returns the document or documents to write to the target index.</p><p>Implementations must preserve AOSC correctness requirements:</p><ul><li>output must be deterministic for the same input document and script parameters</li><li>routing changes must be compatible with the target index shard layout</li><li>emitted documents must preserve IDs unless the extension intentionally documents a different contract</li><li>failures must propagate to the migration instead of being swallowed</li></ul><h2 id="compatibility" tabindex="-1">Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;Compatibility&quot;">​</a></h2><p>Extension APIs are best-effort stable within an OpenSearch major release line. OpenSearch major-version migrations may require extension changes because OpenSearch plugin APIs can change across major versions.</p>',16)])])}const h=t(a,[["render",r]]);export{m as __pageData,h as default};
